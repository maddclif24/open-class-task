extends ./layouts/app.pug

block content
  form(method='post' action=`/edit-news/${newsId}`)
    div
      input(type="text" name="title" placeholder="Title" required)
    div
      textarea(name="body" placeholder="...Edit News" required)
    div
      input(type="submit" value="Edit")